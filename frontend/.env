# Frontend environment variables for local development
# API base URL used by the client (must point to backend)
VITE_API_BASE_URL=http://localhost:8080

# Frontend dev server port (matches package.json dev script)
PORT=3000
VITE_API_BASE_URL=http://localhost:8080
VITE_APP_NAME=AI Detection
VITE_APP_VERSION=1.0.0

